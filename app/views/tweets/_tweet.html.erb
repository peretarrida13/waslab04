<div id="<%= dom_id tweet %>">
  <p>
    <strong>Author:</strong>
    <%= tweet.author %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= tweet.content %>
  </p>
  
  <p>Created At:</p>
  <%= tweet.created_at.strftime("%A, %Y-%m-%d %I:%M.") %>

</div>
